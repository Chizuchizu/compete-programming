#define _GLIBCXX_DEBUG

#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
    int N;
    cin >> N;
    int W[N + 1], B[N + 1];
    rep(i, N) cin >> W[i + 1];
    rep(i, N) cin >> B[i + 1];


    return 0;
}
